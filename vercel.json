{
  "framework": "vite",
  "buildCommand": "cd family-budget && npm install && npm run build",
  "outputDirectory": "family-budget/dist",
  "installCommand": "echo 'skipping root install'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
